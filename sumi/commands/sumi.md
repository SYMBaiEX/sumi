---
name: sumi
description: Create, edit, delete, or run recurring summary tasks. Invokes the sumi manager skill.
argument-hint: "[create | edit | delete | run <task-id> | list]"
---

Invoke the `sumi` skill at `skills/sumi/SKILL.md` and follow its flow. Pass `$ARGUMENTS` through — if the user typed `/sumi run team-weekly`, the skill should interpret that as "run the task team-weekly now."
